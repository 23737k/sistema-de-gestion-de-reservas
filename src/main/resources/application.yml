spring:
  application:
    name: sistema-de-gestion-de-reservas

  datasource:
    url: jdbc:mysql://localhost:3306/sigere
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

backend:
  api:
    base-path: /api/v1
